import { IMapper } from "./IMapper";
import { bookOrder } from "../model/book.model";
import { bookBuilder } from "../model/builder/book.builder";
// export class JSONBookMapper implements IMapper<string[],bookOrder>{
//     map(data : string[]):bookOrder{
//         return bookBuilder.newBookBuilder().

//     }
// }